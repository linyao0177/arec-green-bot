[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
# Stop the build only on real errors: syntax, undefined names, unused imports.
select = ["E9", "F"]
